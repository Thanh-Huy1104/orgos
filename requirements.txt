# Core
crewai>=1.0          # agent engine (uses litellm under the hood for model routing)
pydantic>=2          # RoleSpec / TaskBrief / HandoffEnvelope schemas

# examples/quant_pair_scanner.py
numpy>=1.24          # price series + OLS in the cointegration tool
statsmodels>=0.14    # optional: real ADF test (falls back to a correlation proxy if absent)

# ── LLM provider keys (set whichever provider your models use) ────────────────
# This project is provider-agnostic via litellm. Pick models per role (see
# .env.example), then export the matching key:
#   OpenAI     gpt-*                 -> OPENAI_API_KEY      (platform.openai.com)
#   Anthropic  anthropic/claude-*    -> ANTHROPIC_API_KEY   (console.anthropic.com)
#   Gemini     gemini/*              -> GEMINI_API_KEY      (aistudio.google.com)
# Keys are read from the environment by litellm — no package install needed.
yfinance
