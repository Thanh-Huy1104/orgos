# Core runtime dependencies for the Agent Scrum Team Platform.
# For a full editable install (with the `orgos` CLI on PATH), use:
#   pip install -e .
# This requirements file is provided as a fallback for environments that
# prefer plain pip.

crewai>=1.0          # agent engine (uses litellm for provider routing)
pydantic>=2          # schemas for RoleSpec / TaskBrief / HandoffEnvelope
ruamel.yaml>=0.18    # persona YAML frontmatter parsing

# ── Development ──────────────────────────────────────────────────────────
# pytest>=8

# ── LLM provider keys ────────────────────────────────────────────────────
# litellm reads keys from the environment. Set one or more depending on
# which model(s) you use. Common options:
#   DeepSeek     deepseek/deepseek-chat     -> DEEPSEEK_API_KEY
#   OpenAI       gpt-*                      -> OPENAI_API_KEY
#   Anthropic    anthropic/claude-*         -> ANTHROPIC_API_KEY
#   Gemini       gemini/*                   -> GEMINI_API_KEY
#
# Put the key(s) in a .env file at the repo root (auto-loaded by the CLI):
#   DEEPSEEK_API_KEY=sk-...

# --- Governance substrate (private, local sibling checkout) ---
# The spawn engine now lives in agentkit; install editable from the sibling:
#   pip install -e ../agentkit
